# PDF-grounded evaluation and context investigation

Replace TAPAS-first presentation with an independently transcribed, page-cited OpTC red-team reference. Resolve explicitly identified agents by host/date/PID and observed C2 actor identity; ambiguous UUIDs stay unknown. Keep artifact and endpoint roles separate from compromised processes; failed injection targets are not positive. Unlisted processes are unknown. Report positive recall, prediction uncertainty and POI-excluded results, never a fabricated closed-world accuracy. Keep the prior public benchmark only as an explicitly secondary comparison.

The uploaded ZIP labels DARPA E3/E5, not OpTC. Match UUIDs to available local databases and existing actual pruning outputs. Report candidate coverage separately from retention and classifier accuracy; unavailable datasets remain unavailable. ZIP labels do not define ground-truth paths.

The algorithm addition is an evidence-preserving context subgraph inspired by CONTEXTS conditional expansion and task/process segmentation: derive waypoints from detector evidence, retain exact creation-chain and behavior witnesses, group repeated typed interactions while preserving all event IDs, and block expansion through shared resources. This is a complementary investigation output, not a replacement for full candidate logs or proof of maliciousness. No PDF/ZIP labels enter this construction. Compare output size and PDF evidence retention with existing pruning across all four windows and fixed parameter ablations. Keep rule detector default unless measured evidence supports replacement.

Read all supplied primary papers to the extent accessible, record full-text versus abstract-only scope, and document differences rather than claim reproduction or global optimality. Preserve unrelated workspace changes. Test identity ambiguity, failed-injection semantics, source hash binding, label isolation, evidence preservation and graph exports; run browser checks before GitHub synchronization.
